config {
  type: "view",
  schema: "staging",
  description: "Cleaned version of orders",
}

select
  id as order_id,
  CAST(total AS FLOAT64) AS value_total,
  CAST(created_at AS TIMESTAMP) AS order_date
from
  ${ref("raw_dooca_Orders")}